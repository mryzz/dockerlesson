# Use the official Ubuntu image as the base
FROM ubuntu:latest

# Update the package list and install any packages (example: curl)
RUN apt update && apt install -y curl

# Set a working directory
WORKDIR /app

# Optional: Copy files from the host into the container
# COPY . /app

# Define a default command (e.g., start an interactive shell)
CMD ["bash"]
